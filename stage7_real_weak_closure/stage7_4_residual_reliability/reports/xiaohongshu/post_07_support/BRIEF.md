# Post 07 出图简报 · 「修补前」收束篇

本篇以 4 张 1080×1350 图卡完成第一季：**0（震住）→ 规则（先冻结）→ 方格（数出来）→ 碑文（封卷）**。

## 视觉总纲

- 延续浅黄纸、墨黑大字、黄马克笔、橙红批注、山形页脚和右上引号。
- 角标：`「修补前」· 收束篇`。
- 统一页脚：`180单元 · 720判定 / 有序19 · 方向相反204 / 判定闭集冻结在读残差之前`。
- 不绘制任何实测曲线。唯一曲线位于图 2，只表示预先冻结的期望机制，必须标为 `机制示意，非实测曲线`。
- 真实结果只用冻结计数和可逐格复核的方块表达。

## 01 封面

- 小字：`90个主判定里，“有序”——`
- 超大数字：`0`
- 高亮：`而“方向相反”，有41个。`
- 收束：`我造的风险指南针，指反了。`
- 可用扁平指南针点缀；不增加数据模块。

## 02 考试规则 · SCHEMATIC

- 标题：`考试规则，在看卷子之前就冻结了`
- 副题：`机制示意 · 不是结果图`
- 三步机制链：`给每个像元打“危险分”（1−cos_i）` → `先拒掉最危险的` → `看留下的误差降不降`
- 第一环的解释写作 `越不正对太阳，分越高`；图内用 `保留比例`，不混用 coverage；纵轴明确为 `MAE`。
- 明示适用域：`只在已有可评分支持的像元内排序。`
- 仅允许一条虚线下行的期望曲线，明确标注 `如果指南针有用，应该长这样` 与 `机制示意，非实测曲线`。
- 机制区所有关键说明不小于 24 px。
- 展示冻结的五类 verdict 闭集：有序、无区分、方向相反、支持不足、分数退化。
- 高亮句：`“方向相反”也是合法结果——不许事后改分数。`

## 03 真实判定计数

- 标题：`180个主判定跑完，指针这样倒`
- 标签：`真实判定计数 · 非示意`
- 副题：`主判定（所有可评分像元）· B4/B5各90；每格＝1景×1空间折`。
- 两个 10×9 网格，每格代表一个主判定，按类别聚合排布而不编码时间或空间顺序：
  - B4：有序 0、无区分 25、方向相反 41、支持不足 24、退化 0。
  - B5：有序 1、无区分 48、方向相反 17、支持不足 24、退化 0。
- B5 唯一墨绿格标注 `唯一的“有序”`；B4 图例用空墨绿框标注 `有序 0个`。
- 全部 720：有序 19、无区分 177、方向相反 204、支持不足 248、退化 72。
- 收束：`在方向可判的结果里，我标成最危险的地方，当前基线模型往往错得更少。`
- 边界：`单ROI · 空间折不是独立样本 · 仅作描述，不解释为什么`。

## 04 季终碑文

- 标题：`修补前的账本，到此全部冻结`
- 黑色冻结块：
  - `0.92%      几何拒绝的下界（第六篇）`
  - `3景失败记录  保留在案，未删（第五篇）`
  - `判据边界    仅覆盖自阴影/近零入射，不含投射阴影等`
  - `1−cos_i    本ROI：不构成可靠的风险排序代理`
- 中下部以程序验收与科学结果对照：
  - 标题：`程序关卡全过，排序假设没过`。
  - `规则先冻结 · 交叉审计通过`；`六项 reconciliation PASS · 测试17/17`。
  - 固定网格 `q=75% → 25%`：两个端点都可达的主曲线 114 条；93 条 MAE 上升、21 条下降；配对逐曲线 MAE 百分比变化中位数 `+18.3%`。
- 高亮：`回答要有证据，拒答也要有证据。`
- 收尾：`第一篇：背阴坡不是黑。` / `第七篇：连“哪里更危险”也不能靠直觉。`
- 不再保留“下一季”引子。

## 冻结来源

- Stage 7.4 聚合判定：`outputs/risk-proxy-aggregation-table-v1.json`。
- Stage 7.4 固定网格 MAE：`outputs/risk-proxy-risk-curve-table-v1.json`。仅取 `point_kind=fixed_grid`、`scope_role=primary`、`curve_scope=unit_all_scored`、`grid_status=reachable` 且 `target_q∈{0.25,0.75}` 的行，按 `[acquisition, band, fold, curve_scope]` 配对；两端都存在才进入 114 条分母。逐曲线变化为 `100 × (MAE_q025 / MAE_q075 − 1)`，再取中位数。
- 180 单元：`evidence/risk-proxy-unit-status-ledger-v1.json` 的 `rows`。
- 冻结规则、五类 verdict 与 `1-cos_i`：`docs/risk-proxy-protocol-v2.md`、`configs/risk-proxy-config-v2.json`。
- 结果盲时序：`evidence/result-blind-declaration-v1.json`、`evidence/risk-proxy-runtime-freeze-audit-v2.json`。
- 3 景保留记录：Stage 7.1 `evidence/observation-evidence-manifest-v1.json`。
- 0.92% 下界与判据边界：Stage 7.2 `evidence/support-loss-ledger-v1.json`、`evidence/mconf-registry-v1.json`、`docs/mconf-subprotocol-v1.md`。

冻结证据、配置、文档、输出和脚本只读；新文件仅写入 `reports/xiaohongshu/post_07*`。
