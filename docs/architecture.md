# 《山地遥感物理基座·架构文档 v3.3》

- **正文地位：** canonical architecture
- **版本：** v3.3
- **生成日期：** 2026-08-14（v3 正文冻结于 2026-07-20）
- **canonical Markdown：** `docs/architecture.md`
- **冻结阅读版：** `docs/releases/山地遥感物理基座-架构文档-v3.3.pdf`（派生出版物，不是第二真相源）
- **版本语义：** v3.1、v3.2 与 v3.3 均只增不撤——不撤销、不替换 v3 的任何主张、定义或框架，章节编号与既有引用全部保持有效。既有节点合同、schema 与证据文件中对「架构 v3 §X」「v3.1 §X」「v3.2 §X」的引用无需改写。修订内容见 §9.1、§9.2、§9.3。

## 0. 立场、范围与诚实边界

山地遥感物理基座以世界坐标中的物理状态为中心：图像外观是证据，不是最终表征；物理一致性高于纹理一致性。架构的目标是在复杂地形、几何畸变和异质光照下，形成可追溯、可迁移并带有不确定性的规范物理状态估计。

这不是“用极简可微模型取代一切传统处理”的主张。大气散射、多模态观测、时间先验和工程算力都仍是实际约束。架构的主张是：把不可回避的物理放进由真实观测约束的联合闭环，并按变化速率与依赖关系管理自由度，以便把误差来源、不确定性和失效边界显式暴露出来。

本文件不把仍未验证的机制写成结论。特别是 Stage 6.5.3-B 仅是当前机制级压力测试：它不能验证完整 L0-L5 架构；三种方法、mask、residual baseline、reliability 与 risk-coverage 的定义都属于独立节点实验合同，不由本文件猜测或补齐。

### 0.1 可证伪的胜负标准

本范式成立与否，由下列三条实验回答，不由辩论回答：

1. 同样吃进全部不可回避的物理，联合可微闭环给出的状态估计精度，是否显著高于开环级联处理？
2. 它给出的不确定性量化是否真有价值——即能否在传统级联流程给不出的地方，正确区分「有观测支持的推断」与「无支持的外推」？
3. 它能否在可接受的算力、数据与运维成本下大尺度落地？

前两条的先验判断偏向为是：联合标定优于级联标定是逆问题领域的常识，不确定性是山地应用的刚需。第三条尚未被证明，是本范式必须用实验回答的开放问题，与未决问题 E 同指一事。

这三条是整体验收判据，不替代 §8 对单步声称的约束：任何一步实验只能声称其实际检验的机制，不得以本节三条为名越级声称。

## 1. 设计原则

### 1.1 三个优先级

- **世界优先于图像：** 基元是世界坐标下的位置与观测关系，而不是孤立像素。
- **状态优先于外观：** 系统估计的是有物理语义、来源和不确定性的状态；外观只作为证据进入推断。
- **物理一致性优先于纹理一致性：** 训练与评估应检验状态经正向观测模型能否解释观测，而不是只追求视觉重建。

### 1.2 证据优先与未决义务

> 通过 QA、配准、定标与不确定性校验的有效观测，高于统计先验；观测自身不可信或模型解释不足时，系统必须保持未决，不得强行听从任何一方。

因此 L4 统计过程模型只能提供可追溯的软先验或初始化，不能伪装成当期观测反演。系统也不得为了输出完整图面而把无支持区域涂成确定结果。

### 1.3 阻力最小路径与多解陷阱

当多种物理解释可以同等拟合观测时，优化会偏向代价最低而非物理正确的解释。这一“阻力最小路径”是贯穿各层的审查规则：每次新增状态、传感器、先验或损失项，都必须说明其可能制造的等价替代、隔离机制与可证伪检验。

不允许以空间平滑掩盖状态边界，也不允许未经物理约束的跨模态学习直接改写物理状态定义。几何、可见性、辐射与状态之间的依赖必须可追溯。

### 1.4 明确拒绝的范式

下列做法已被判死，不因换一种说法而重新可用。新提案若实质等价于其中任何一条，应直接驳回：

- **在二维骨干上贴地形适配器。** 治标：畸变的来源在数据与成像几何里，不在网络末端。
- **以掩码纹理重建作为预训练目标。** 它会把光照与几何畸变一并学进表征，正是 §1.1「物理一致性优先于纹理一致性」要排除的。该做法是遥感自监督的主流路径之一，因此必须明文拒绝——只靠正面表述隐含排除挡不住它换个名字回来。
- **「校正后即弃」。** 丢掉几何与光照信息，等于丢掉解耦所需的证据。
- **一步到位做全球覆盖。** 三维数据底座尚不存在；覆盖范围不能替代证据支持。
- **让稳定几何底座 G 与时变状态 X(t) 共享同一计算图同时求导。** 高维参数会互相掩护、等价代换，产生比单变量误判更隐蔽的多解（§1.3 的具体应用）。
- **让跨模态数据以未经物理约束的学习方式直接连入 L1/L2 优化图。** 只能作为隔离的预处理候选（§2.3）。

拒绝清单与正面原则的作用不同：正面原则说明应当怎样，拒绝清单锁死已经判死的路径。两者都需要。

## 2. 规范状态与 L0-L5 分层

“真值”在本架构中不是对模型内部状态的泛称。除独立地面测量外，L0/L1 输出一律称为**带不确定性的规范物理状态估计**；独立地面测量才可称为真值或参考真值。

### 2.1 状态层：G、X(t)、Z(t)、N(t)，与外生输入 E

- **G：稳定几何底座及其几何不确定性。** 它提供世界坐标、地形与长期稳定结构的规范表示；常态训练中锁定，不作为任意梯度吸收器。
- **X(t)：目标时变地表状态。** 例如含水量、植被覆盖度（VCF）及其他经实验合同定义的时变状态；它们可随时间被有效观测约束而更新。
- **Z(t)：独立观测得到的结构约束。** 例如 `canopy_height`；它与 G、X(t) 的来源、刷新节奏和不确定性分开记录，不能被悄悄折进某个状态项。
- **N(t)：干扰或标定变量。** 包括大气、辐射定标、反照率/BRDF 和传感器偏差等。N(t) 不得被错误归因给 G 或 X(t)。
- **E：外生输入。** 采集时即已确定、精确已知、不参与推断的条件量。例如太阳-传感器几何（太阳高度角/方位角、观测天顶角/方位角）、成像时刻、轨道与斜距几何。**E 不是状态**：它逐次观测从采集元数据直接读取，不建模其时间演化，也不需要被观测约束更新。E 进入 L2 正向观测合成作为条件（§2.4），但**不占推断自由度、不接受梯度、不吸收残差**；任何把 E 当作可拟合量的做法都是把已知量当未知量，属分类错误，与 §1.3 的多解陷阱同源。E 与 N(t) 的分界是**是否需要估计**：太阳角已知，故属 E；大气透过率需估计，故属 N(t)。

DSM-DEM 的主要意义是提供冠层或地物高度约束，不是“粗糙度硬边界”。光学有效粗糙度与 SAR 尺度粗糙度必须分别定义；在没有独立证据前，它们仍是未决或受限变量，不能由 DSM-DEM 直接宣称为已锁定。

### 2.2 L0：世界坐标观测与几何底座

L0 以世界坐标位置、原始观测及其完整采集几何为核心。每条原始观测保留时间、传感器、太阳/视角、配准与来源信息；原始观测永久保留，不因派生状态失效而删除。

**证据成员身份与操作资格相互独立。** 一次真实发生的 acquisition，即使不满足当前拟合、训练或验证任务的资格，仍然属于观测证据。操作资格是任务级的、可随任务变化的投影；证据成员身份一经确立，在同一 evidence universe 版本内不可撤销。任何操作层的拒绝都**不得反向删除或改写证据成员身份**，也不得使该 acquisition 从机会分母中消失——被拒绝的观测必须以带原因的终态留在账本里，而不是变成缺失。本原则跨光学、SAR 与无人机重建同样成立。

**「永久保留」的对象是证据身份与可追溯性，不必然是本地热存储字节。** 必须永久保留的是：acquisition identity、source locator、provenance、checksum/hash、观测与支持元数据，以及派生关系。原始字节分两种情形处置：上游源稳定且可复现获取时，允许冷存或只保留可验证引用；源不可重现、易失或已经过不可逆处理时，必须归档副本。**「可复现获取」是一项需要验证的主张，不是可以假设的属性**——至少须有一次按 locator 重新取回并通过 checksum 比对的记录；该主张失效时（上游产品下线、集合弃用、定标世代变更），相应数据自动转入必须归档副本的情形。本条澄清上段「永久保留」的含义，不放宽其对证据可追溯性的要求。

DEM、DSM、LiDAR、光学、多光谱、SAR/InSAR 及无人机多角度重建都可作为证据来源。无人机 SfM/MVS 或 AI 重建得到的点云、网格和纹理是 L0 观测来源之一，必须独立记录地面控制点/RTK 校正与重建质量，不能把重建缺陷混同为光照或遮挡问题。

### 2.3 L1：时变地表状态估计

L1 负责以 X(t) 为中心的时变状态估计，并消费 Z(t) 的独立结构约束与 N(t) 的标定/干扰解释。它不是把所有不一致都压入三个固定状态的容器，也不把状态估计误称为真值。

新增传感器若要影响状态，优先通过可审计的物理映射进入观测证据链。未经物理约束的 AI 翻译或重采样只能作为隔离的预处理候选，不能直接改变 L1/L2 的状态空间或梯度定义。

### 2.4 L2：几何与物理正向观测模型

L2 把 G、X(t)、Z(t)、N(t) 与采集几何（即 §2.1 的外生输入 E）组织成正向观测合成。本文档中“渲染”只指 L2 的正向观测合成；对几何变化的操作一律称为**局部几何重建/替换**，不再称为局部重渲染。

光学与 SAR 的可见性和失效机制分别建模。光学中的阴影、遮挡与辐射条件不能直接套用到 SAR 的叠掩、透视收缩和雷达阴影；SAR 专属的确定性屏障与可微正向算子仍是未决问题。

### 2.5 L3：受质量因子约束的自监督学习

L3 在可解释的有效支持范围内比较真实观测与 L2 合成观测，学习或更新受合同允许的状态。质量因子可以被编译为有效权重，但不能抹去其来源、作用时点或不确定性。零支持与模型不充分不是可被损失函数静默修复的噪声。

### 2.6 L4：时空过程模型

L4 按过程与时间尺度提供软先验、候选初始场或情景信息。L4 初始化必须标注来源和高不确定性，绝不伪装为已由观测反演出的结果；后续有效观测应能覆盖、修正或维持未决。

L4 的快慢变量冲突仍未解决：例如快速湿润事件与季节性先验相冲突时，系统尚无经过验证的判别阈值与恢复机制。

### 2.7 L5：下游适配与评测

L5 只在带来源和不确定性的规范状态估计上进行任务适配与评测。验证必须采用跨域断裂（Cross-Domain Isolated Validation）：训练区与留出区应在气候带、地质背景或植被类型上存在真实断裂，不能用同域随机切分替代外推检验。

## 3. 质量、支持与不确定性

质量不再由一个不可拆解的 Mconf 数字代表。每个输出及其有效权重必须保留至少下列可追溯因子：

- **support_mask：** 该位置是否具有可用于当前推断的观测支持。
- **geometry_visibility：** 给定几何与观测方向下的可见性、遮挡或阴影条件。
- **reconstruction_quality：** 几何重建本身的完整性、控制点校正、拼接与伪影风险。
- **sensor_quality：** 传感器、QA、噪声、饱和、云等观测质量信息。
- **model_adequacy：** 当前正向模型能否充分解释此处观测的诊断。
- **output_uncertainty：** 状态或产品输出的不确定性及其传播结果。

六类因子按其在推断链条上的位置分为两组：

- **推断前证据质量**：`support_mask`、`geometry_visibility`、`reconstruction_quality`、`sensor_quality`。它们描述观测本身的可用性，可以决定某次观测是否允许进入当前损失。
- **推断后诊断**：`model_adequacy`、`output_uncertainty`。它们是推断过程产生的结果，不是推断的输入条件。

**推断后得到的 `model_adequacy` 或 `output_uncertainty`，默认不得反过来静默降低产生它们的同一次观测的残差权重。** 若需采用异方差似然、鲁棒损失或类似机制，必须由独立节点合同定义其形式、适用域与验证方式，不得作为实现细节引入。此禁令封堵的是一条具体的退化通路：模型对某处解释不佳 → 宣称该处不确定 → 据此降低该处损失权重 → 该处永远不被学习，而各项汇总指标看起来都在改善。

任务可在明确记录编译规则后从这些因子生成 `effective_weight`，但不得只保存该合成数值。各因子须可回溯到输入、算法版本、适用范围与时间。

此处的「保留」指**信息不丢失且可回溯，不要求为每个产品物理复制一份质量栅格**。允许的实现方式包括：把因子值内嵌为产品字段、引用共享的质量资产、或引用可复算的算法与输入版本。同一 acquisition 的 `sensor_quality` 被十个产品使用时，不应复制十份。架构约束的是信息不能丢，不是文件必须复制。

**观测支持谱（`observation_support_profile`）。** `support_mask` 只回答「此处当前有没有合格支持」，回答不了「此处究竟被现实世界反复约束过多少次、以多大的条件多样性」。因此每个世界位置还须可追溯下列最小四项：

- `observation_opportunity_count`：该位置进入观测机会的次数，无论是否形成有效支持。
- `valid_support_count`：其中形成合格支持的次数。
- `support_gap_reasons`：未形成支持的原因分解，沿用既有 reason-code 体系。
- `support_diversity_summary`：对已有支持的条件多样性作出描述，至少覆盖三方面——时间条件是否重复、观测几何是否重复、传感器来源是否重复。

本条只要求证据数量与证据多样性**可追溯**，**不预定义如何把它们压缩成一个分数**。本版明确不定义 `effective_independent_support`、`direct_support_fraction`、任何加权公式或全局置信度：目前尚不知道十次太阳角相近的观测等价于几次独立证据，提前给出公式是假严谨。该折算关系应由后续节点用真实数据校准后再议。

所有输出必须标注为 `observed/inferred`、`prior_only` 或 `unsupported`。其中 `support_mask = 0` 或无合格观测支持时，只允许标为 `prior_only` 或 `unsupported`，不得标为 `observed/inferred`。

- **observed/inferred：** 有合格观测支持并经模型推断；该标签始终表示「有观测支持的反演结果」，**不表示直接测量或真值**。
- **prior_only：** 仅由先验或初始化提供，保留来源与高不确定性；
- **unsupported：** 没有足以形成状态结论的支持。

上述三类来源类别与本节前面的推断前／推断后分组**相互正交**：前者回答「这个输出有没有观测支持」，后者回答「某项质量信息处在推断链条的哪一端」。两套分类不对齐、不互相映射，也不得合并为单一分类体系；同理，质量因子本身不属于 §2.1 的 G/X(t)/Z(t)/N(t)/E 身份类别——那套类别只适用于进入推断图的状态量、待估参数、独立结构约束与外生条件量。

无观测区与主动清零区必须分别量化。前者源于观测稀缺或不可获得；后者源于几何、质量或模型充分性判断主动拒绝使用观测。两者的覆盖率、外推误差与校准度不得混成一个“低置信度”总体数字。

## 4. 几何更新与依赖感知失效

几何变化不触发包围盒内历史状态的无条件全删。局部几何重建/替换应遵守以下边界：

- 原始观测永久保留，并保留其原始坐标/采集语义与来源。
- 受坐标映射、可见性或辐射关系变化影响的**派生状态**进入 `invalidated` 或 `quarantined`，并说明失效原因。
- 只对确实失效的派生状态重建；未受影响的状态不因空间邻近而被改写。
- 影响范围必须包含几何变化导致的可见性与辐射传播区域，而不只是几何包围盒。
- 不使用空间平滑掩盖新旧状态边界。边界的不确定性、来源和失效状态必须显式保留。

若 L4 为重建区域给出初始化，它仅能生成带 `prior_only` 来源与高不确定性的候选。它不得替代有效观测，也不得把初始化伪装成 L1 的观测反演历史。

## 5. 合成、渲染器与数据责任

Mitsuba/Eradiate 等高保真渲染器当前限定为**离线参考与合成数据源**。这是本项目基于遥感物理覆盖、算力和集成成熟度作出的工程裁决，不是对 Mitsuba 3 技术上是否可微的否定。只有经过专项可行性验证后，相关技术才可作为 L2 候选后端。

所有合成数据必须记录 simulator provenance、输入场景、参数版本、标签生成方式与 domain gap 说明。合成数据不得自动视为真实标签，也不得同时被当作训练来源和独立验证真值。独立验证仍需要与训练来源隔离的观测或地面参考。

## 6. 数据与验证策略

- 从地貌反差明显的小样区开始，逐步验证，而不以架构文字代替实验。
- 用独立地面测量、机载 LiDAR、GNSS 和可审计的参考观测来标定或验证；它们与模型状态估计的地位必须区分。
- 对无观测、主动清零、几何遮挡、重建低质量、持续大气遮挡等区域分层报告覆盖、误差和不确定性校准。
- 共享的是地形条件到物理状态的可学习关系，不是把一个区域的具体反照率或状态数值覆盖到另一区域。
- 任何“完整覆盖”产品都必须随输出来源类别、质量因子和不确定性一并交付。

## 7. 已知失效域与未决问题 A-I（不假装解决）

### A. 遮挡边界的信息缺口

主动拒绝不可信观测能阻止错误归因，却会形成信息缺口。需要量化其覆盖、成因、先验依赖和补观测可行性，而不是把清零当成问题已被解决。

### B. SAR 正向算子的可微化与专属可见性规则

SAR 的相干成像、斑点、介电特性以及叠掩/透视收缩/雷达阴影均不能直接沿用光学规则。可微化与独立质量机制仍是开放问题。

### C. 无观测区的外推幻觉

没有观测且缺少相似训练样本时，系统可能给出貌似合理但不受现实约束的结果。应以跨域断裂验证和来源类别使其可见，而非宣称消除。

### D. 多云区与持续观测稀缺

晴空稀缺是物理约束。补观测是否有效取决于传感器与云/遮挡成因，不能把光学补拍假设为普遍解法。

### E. 大尺度工程可落地性

联合闭环是否能在可接受算力、数据与运维成本下扩展，尚待实验验证。降低耦合复杂度不等于已经完成工程落地。

### F. 状态可辨识性与结构约束的边界

冠层/地物高度约束、X(t) 状态、光学有效粗糙度与 SAR 尺度粗糙度的独立定义、刷新节奏和可辨识性尚未关闭。DSM-DEM 不能替代这些独立证据。

### G. L4 快慢变量冲突

快速真实变化、传感器噪声与定标误差发生冲突时，何时接受观测、何时保持未决、如何恢复先验影响，尚无经验证的判定与评测协议。

### H. 局部几何重建/替换的触发与影响范围

如何区分持续几何变化与随机残差，如何界定包含可见性与辐射传播的影响范围，以及如何审查和回滚，仍需小样区经验与专项验证。

一个候选方向（**尚未验证，不构成已关闭的方案**）：对同一世界位置，比较其在若干次**太阳角相近**的观测下、实测与模拟之间的残差时序；随机波动视为观测噪声，持续处于高值的阶跃则视为几何可能已真实改变。控制太阳角相近是为了隔离光照差异，使残差变化更可能来自几何本身。该指征只提供判断依据，**不自动触发任何重建或替换操作**。阶跃的判定标准、影响范围的界定与误触发的回滚机制均仍未确定。

### I. 主动清零区的长期信息缺口

主动清零与被动无观测应分层量化。几何遮挡、重建质量、持续大气遮挡等不同来源的补观测路径、风险与评价口径仍待制定。

## 8. 推进约束

最小闭环应先验证可追溯的观测支持、质量因子、状态来源与不确定性，再扩大到多区域、多模态、L4 和下游任务。每一步只能声称其实际检验的机制，不可越级声称完成整套架构。

本版不创建 Stage 6.5.3-B 的配置、执行器、结果或报告，不运行任何实验，也不把该节点的未定方法写入架构正文。实验方法、mask、residual、reliability 与 risk-coverage 的精确定义必须在独立节点合同中经证据与裁决后形成。

## 9. 版本说明与出版纪律

v3 保留 v2 的世界坐标优先、状态优先、物理一致性、阻力最小路径警惕、L0-L5 分层、跨域断裂验证、L4 快慢变量冲突、无观测/主动清零区量化、无人机重建、质量分离、离线高保真合成数据源及未决问题 A-I。

相对 v2，v3 的实质修正是：以 G/X/Z/N 代替把误差压入三个 L1 状态；撤销 DSM-DEM 对粗糙度的硬边界主张；以六类可追溯质量因子取代单一 Mconf；明确 `observed/inferred`、`prior_only`、`unsupported` 来源类别；采用依赖感知的失效与隔离，而不是包围盒全清；并将渲染器、合成数据和 Stage 6.5.3-B 的主张收紧到其真实证据边界。

### 9.1 v3.1 修订说明（2026-08-08）

v3.1 是**只增不撤**的修订：不撤销、不替换 v3 的任何主张、定义或框架，章节编号与既有引用全部保持有效。

本次补回三处 v2 内容。它们既不在 v3 上文声明的保留清单、也不在撤销清单，属于重写 v3 时被压缩掉、从未经过裁决的内容——2026-08-06 经逐节比对识别，2026-08-08 由所有者裁决补回：

- **§0.1 可证伪的胜负标准三条**：v3 原只保留其中第 3 条作为未决问题 E，第 1、2 条无对应。
- **§1.4 明确拒绝的范式**：v3 原已吸收其中两条（见 §1.3、§2.3），另四条无对应。
- **§7 H 的时序残差判别候选方向**：v3 原只保留问题陈述，未保留候选方案。

第四处——v2 记录的「重置区边界空间平滑」被否决提案及其否决理由——经裁决**不进正文**：它是决策案例而非架构原则，留存于 `docs/architecture-v2-residuals.md`。§4「不使用空间平滑掩盖新旧状态边界」这一结论在 v3 中已有，不受影响。

补回内容的措辞已适配 v3 框架。v2 原表述建立在单一 Mconf、三状态皮肤与「局部重渲染」之上，这些均已被 v3 撤销；此处只继承其判断与思路，不继承其框架。

来源 PDF（XeTeX 生成，内部 CreationDate 2026-07-16，早于 v3）已按退役物归档纪律移出仓库，见 `_ops/2026-08-06-v2-architecture-retirement/`。

### 9.2 v3.2 修订说明（2026-08-09）

v3.2 同样是**只增不撤**的修订：不撤销、不替换 v3 或 v3.1 的任何主张、定义或框架，章节编号与既有引用全部保持有效。

本次只做一件事：为 §2.1 补入第五类 **E（外生输入）**。

补入理由不是引入新概念，而是**正式命名架构已在使用却从未定义的一项**：§2.4 原文即写作「G、X(t)、Z(t)、N(t) **与采集几何**」，将其与四类并列；§2.2 也要求 L0 完整保留采集几何。此前它有位置、有职责，却无身份、无边界。

触发本次修订的是一次真实的分类事故：Stage 7.5 合同要求「注册全部输入…逐项声明 G/X/Z/N 类别」，而太阳角、采集几何在四类中无一可诚实归入——G 要求长期稳定、X(t) 是待推断目标、Z(t) 是地表结构约束、N(t) 是待估计干扰且明令不得错误归因。该合同将 X 重定义为「外生观测或强迫量」、把 X 的原义挤给 Z，形成与 §2.1 相反的词表；Stage 7.6 的排除项亦写成「不联合更新 G 与 Z」，与 §1.4 拒绝律原文「G 与 X(t)」不一致。本节只记录该结构缺口与该处词表颠倒并存，不断言起草动机或二者的因果先后。该颠倒在 Stage 7.5 激活前经复审发现，identity registry 尚未冻结，未产生下游污染。

E 的引入使四类各自的定义回到 §2.1 原文，无需重定义任何一类。

### 9.3 v3.3 修订说明（2026-08-14）

v3.3 同样是**只增不撤**的修订，定位为证据支持与可实现性补丁：不撤销、不替换 v3、v3.1 或 v3.2 的任何主张、定义或框架，章节编号与既有引用全部保持有效。

本次只解决四件现实问题：

1. **§2.2 增加「证据成员身份与操作资格相互独立」。** 来源是 Stage 7.1-R 的实证结果：21 景全部保留证据成员身份，其中三景因质量归因未进入当次操作，但成员身份未被撤销、也未从机会分母中消失。该区分跨光学、SAR 与无人机重建同样成立，故上升为架构条款；具体的景数、split 划分与 mask 命名仍留在节点合同中，不进正文。

2. **§3 增加观测支持谱的最小接口。** 只规定四项必须可追溯的内容，并明确不定义任何压缩公式。

3. **§3 增加质量因子的推断前／推断后分组，并禁止推断后诊断静默回改同一次观测的残差权重。** 该禁令封堵的是模型「宣称不确定以逃避学习」的退化通路；异方差似然等机制并未被禁止，但必须由独立节点合同定义与验证。

4. **澄清两处实现边界，避免架构理念拖死工程。** 「永久保留」的对象是证据身份与可追溯性，而非必然的本地热存储字节；「保留质量因子」指信息可追溯，而非为每个产品物理复制栅格。两处均为对既有条款含义的澄清，不放宽其实质要求。

**本次修订不关闭任何未决问题。** 特别地，新增的观测支持谱只是 §7-D（多云区与持续观测稀缺）与 §7-I（主动清零区的长期信息缺口）的**度量接口**，不构成对这两个问题的解决；如何把支持数量与条件多样性折算为有效独立证据，仍然完全开放。

本次明确不做的事，一并记录以免日后重复讨论：不新增第四种产品来源状态（冲突情形先进入诊断字段，不增加状态机复杂度）；不重命名 `observed/inferred`（仅重申其含义为「有观测支持的反演结果」而非直接真值）；不要求正式的依赖图数据结构（只要求 lineage 足以支持失效判断，具体形式由工程实现决定）；不定义全局 confidence 公式；不定义有效独立样本数的数学形式；不把节点级的栅格名、mask 名与 split 划分写入正文；不要求为每次低质量观测永久保存完整原始文件；不继续细拆失败来源枚举——现有质量因子与 reason-coded support 已足以支撑后续实验。

本 Markdown 是唯一 canonical architecture。PDF 仅为由该正文生成、可审阅和可归档的冻结阅读版；任何语义修改必须先更新本 Markdown，再重新生成并校验 PDF。
